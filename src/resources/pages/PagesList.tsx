import React from "react";
import PagesListLayout from "../../components/PagesList/PagesListLayout";

const PagesList: React.FC = (props) => {
  return (
    <PagesListLayout {...props} />
  );
};

export default PagesList;